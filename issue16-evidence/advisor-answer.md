lane: sym_cjcj_llvm_16_implement_r5788821149
答复（主控 0923 12:2x）：

1. **LLVM 包验收入口（例外四，与 cjcj-llvm#13 同口径）**：`kkk2_build_two.sh`/UNIT 三臂/SO 哈希对 llvm_rebase 不适用。替代三臂：① kkk2 上 baseline（冻结 llvm_rebase 5fc812a1）与 candidate 各一份独立 Release 构建 llc/opt/FileCheck（`ops/bin/wf_kkk2.sh build` 槽锁，rc=0）；② lit 同尺差集：基线既有 8 FAIL 已登记 cjcj-llvm#19，候选独红必须 0，新增用例绿；切刀/恢复臂在 lit 层做（切消费端 ⇒ 只有目标用例红，恢复全绿）；③ 编译后读回：托管小程序两臂 rc=0 结果一致。`lane_selfcheck.py` 的 2-diff/SO 行照跑记 rc、报告单列 N/A（tools 债 cjcj#90）。
2. **SD 负载**：源码不在 llvm 仓；canonical ELF `kkk2:/root/sdprov-run/canonical/survival_dense_5fc812a1_90fc0f226248`（sha256 90fc0f22…ba60f），反汇编/计数 `kkk2:/root/synth_be_0923/{sd,main,loop}.asm`（计数表见 `/root/cj_build/reports/SYNTH-BARRIER-ELISION-0923/packages.md` §SD 计数）。本包（B3 静态堆分类）的 SD 目标断言用「重建 SD 后 `g_cjHeapRangeCount` 站数下降、其它类不变」——但**当前可用前端不是 canonical 那枚**（见第 3 点），若无法重建同强度 SD，则以 IR 级正例代替：手写/既有 `.ll` 中可证堆目标的 store 在候选上不再发 heap-range 检查（FileCheck 断言指令序列），不可证目标保持；SD 计数记「未复现，原因」。
3. **托管 PEA/phi 的 SDK 坐标**：染色 SDK `kkk2:/root/sdkdepot/b99430a618af-1ecb811801ca`（CANGJIE_HOME），cjc 宿主 runtime `kkk2:/root/sym_cjcj_48_implement_r5685150408/host/runtime/lib/linux_x86_64_cjnative`（H48），装配见 `kkk2:/root/diff_harness_708/3146928c7624/kkk2_managed.sh`:3-21；#13 棒实测该 SDK 目标 runtime 缺 `g_cjThreadGCDataOffset/LoadBadMaskOffset` 符号，链接改用 `kkk2:/root/sodepot/b7c19036dd611911a4fe050c3d4e8a74f9130c88/` 两枚 SO 后通过。**实体复制**到你自己的 kkk2 目录，只替换 llc/opt（0922 令：不用符号链接、不改 sdkdepot）。⚠ 该 SDK 前端是官方 nightly（2026-07-21），SD 上发射的 store barrier 计数为 0（#13 实测）——所以 PEA 后堆/栈分类的托管正例要自己用小程序构造并 objdump 核，不要指望 canonical 计数复现。
4. 与同文件其它包（#14 B1、#15 B2 改 `storeBarrierFast` 一带）的冲突按内容核，后合者接回；本包只做 B3。
