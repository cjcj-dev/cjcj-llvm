# lane notes · cjcj-llvm#44

坐标：llvm_rebase origin/main `722ae417f6cb99a25286e0a4f310e8a7dc4cd9fb`（回读 rc=0）。
`CJBarrierOpt.cpp` / `CJIRVerifier.cpp` / `Intrinsics.td` / `Function.cpp` 自 `5fc812a1d014` 至 HEAD 无提交。

## barrierarray5

- 参照：`llvm/include/llvm/IR/Intrinsics.td:441-442` `int_cj_gcwrite_struct` 的 size 是 `llvm_anyint_ty`（重载）。
- 打印：`Intrinsic::getName` 经 `Function.cpp:1805-1806` `remangleIntrinsicFunction`，由 `LLParser.cpp:272` 在解析后改名。
- 实测输出（5fc812 opt，N=3，rc=0）：`call void @llvm.cj.gcwrite.struct.p0i8.i64(... %3, ... %7, i8* nonnull %.sub, i64 6)`，下一处仍是 `llvm.memcpy.p1i8.p0i8.i64(... %20, ... %9, i64 6, i1 false)`。
- 旧 CHECK `p0i8(` 不是 `p0i8.i64(` 的子串。操作数不改。保留条件在 `CJBarrierOpt.cpp:612-637`：源不是已证明无引用的结构体时不 `replaceBarrier`。

## nested-array

- 参照：`llvm/lib/Transforms/Scalar/CJIRVerifier.cpp:702-706`。源码已无 `WriteBarrier instruction should be used`。
- 现行文案与 `memcpy-reference-payload.ll:5` 相同，并 `compilation aborted`（`nested-array.ll:37` 的 ABORT 子串仍中）。
