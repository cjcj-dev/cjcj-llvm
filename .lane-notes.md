# lane notes · cjcj-llvm#38

坐标：llvm_rebase origin/main `274a611be85e1cf1019f67e4220ee2097c2bb801` rc=0。
规格来自 `/root/cj_build/reports/REPORT-sym_cangjie_runtime_480_explore_r5831552740.md` 实现任务书 §1。本条只做 cjcj-llvm 半边。

| 参照 | 不变量 | 我方落点 |
|---|---|---|
| Intrinsics.td:533-534 `int_cj_fill_in_stack_trace` 返回 `llvm_gcptr_ty`，参数 `llvm_ptr_ty` + `llvm_gcptr_ty` | canonical RawArray 返回必须是 gcptr，不能是 CPointer | Intrinsics.td 紧挨增加 `int_cj_string_dedup_canonical`，签名相同 |
| CJRuntimeLowering.cpp:96 map safepoint=true；:1069 case 走 `replaceWithRuntimeFunc(CI, false, false)` | 降到 `CJ_MCC_*`，不是 gc-leaf，不是另一组 true/false 标志 | map 增加 `CJ_MCC_StringDedupCanonical` safepoint=true；case 并入 :1069 那一组 |
| ManagedABIGateVerifier.cpp:126 精确名单；:310 intrinsic case | 禁止「凡 CJ_MCC_ 都放行」 | 名单加 `CJ_MCC_StringDedupCanonical`；case 加 `cj_string_dedup_canonical` |

HotSpot TLAB 序列锚：barrierSetC2.cpp:763-780。本条是 LLVM 分支松弛记账，不是 ZGC 相位。

# cjcj-llvm#23 R1

打回：createCJMemsetLoweringLegacyPass 仍在 if (CJPipeline) 内。同一函数里 createCJBarrierLoweringPass 已在开关外。

修法：非 JIT 在 barrier lowering / ISel 之前调度同一个 lowerCJMemset（CJSpecificOpt.cpp:125）。JIT 仍走 createCangjieSpecificOptLegacyPass。不改 verifier。

锚：SelectionDAGBuilder.cpp:5931 只消费 Intrinsic::memset；未知 intrinsic 走 :5833。direct 入口是 TargetPassConfig.cpp:addISelPasses。

# cjcj-llvm#32

坐标：/root/cj_build/llvm_rebase origin/main = f85f5c9d63336dbd7a730eab709c5073c8e7c695（rc=0）。cjcjdev/main 同 sha。

上游 RewriteStatepointsForGC.cpp:638：ExtractElementInst 是标量 BDV，返回自身，不递归向量操作数。
我方 base 推导已同形：CJRewriteStatepoint.cpp:683。
撞断言的是 findAllocaInsts else 白名单（CJGCLiveAnalysis.cpp:105-108），缺 ExtractElementInst。
修法：并入与 LoadInst 相同的终端类，共用随后的 IsGCPtr 尾部。不改 :71，不递归向量操作数，不改 FileCheck。

producer → consumer：
1. 测试 IR extractelement（base-vector.ll:18，scalar-base-vector-2.ll:46）
2. computePHINodeToAllocaInsts（CJRewriteStatepoint.cpp:2141）或 computeFakeGCPtrs（CJGCLiveAnalysis.cpp:252）
3. else 终端分类必须在 :108 之前接受 ExtractElementInst
4. findBaseDefiningValue（CJRewriteStatepoint.cpp:683）与物化（:1112）产出 base_ee / gc-live，由 FileCheck 观察

# cjcj-llvm#41

坐标基于 722ae417f6cb99a25286e0a4f310e8a7dc4cd9fb。参照是同文件上游分路 AArch64AsmPrinter.cpp:1718-1725，不是 ZGC。

producer → consumer：
1. isel 把直接 tail call 选成 TCRETURNdi（AArch64ISelLowering.cpp:6250 允许 C CC；opcode 在 AArch64InstrInfo.cpp:7932）
2. emitInstruction 在 switch 之前调用 tryEmitCangjieSpecificCall（AArch64AsmPrinter.cpp:1548）
3. AsmPrinter.cpp:4172-4176 把 MI opcode 原样传入 emitMccNewObjectFastPath
4. emitMccNewObjectFastPath（AArch64AsmPrinter.cpp:2120）必须在 MCInstBuilder(Opcode) 之前把 TCRETURNdi 降成 B 并 return。本包改点。
5. 未拦截时上游 AArch64AsmPrinter.cpp:1718-1725 做同一降级
6. getCangjieSpecificCallInstSizeInBytes（AArch64InstrInfo.cpp:105-109）在名字分支内先看 opcode，TCRETURNdi 返回 4。消费端是 getInstSizeInBytes:166，再被 branch relaxation 读

不改 LowerSTATEPOINT。emitCangjieCallStubInstImpl:2042-2044 用 TCRETURNdi 对比 BL 选尾调用。
LLVM intrinsic 名：`llvm.cj.string.dedup.canonical`。枚举：`Intrinsic::cj_string_dedup_canonical`。
runtime 符号跟 fill 的 map 拼写，无额外后缀（`replaceWithRuntimeFunc` 用 map 里的名字原样 `declareCJRuntimeFunc`）。
