# lane notes · cjcj-llvm#33

坐标：`f85f5c9d63336dbd7a730eab709c5073c8e7c695`（`git -C /root/cj_build/llvm_rebase rev-parse origin/main` rc=0）。本包不是 ZGC 运行时；规格锚是上游/本树 LLVM。

不变量：进入 `addGCFields` 之前，alloca 必须已由 `computeStructTypeLayouts` 写入 `StructLayoutGCPtrMap`；不属于该表的值在分类层排除。数组 payload 保持未登记、无 struct-live、诊断照打。

| 参照 | 我方 |
|---|---|
| `CJRewriteStatepoint.cpp:2102-2125` `computeStructTypeLayouts` 只登记入口块 StructType alloca | 生产端，不改 |
| `CJRewriteStatepoint.cpp:2986-3007` `isRegisteredSRetCallArgument` 数组 payload 不是已登记槽 | 诊断端，不改 |
| `CJGCLiveAnalysis.cpp:941` `isStackContainGCPtr` | 分类层：基为 AllocaInst、分配类型不是 GC 指针、且 `StructLayoutGCPtrMap.count==0` 则返回 false |
| `CJGCLiveAnalysis.cpp` `addGCFields` 断言 `StructLayoutGCPtrMap lacks V information` | 消费端断言保留，不放宽 |

禁止 `operator[]` 查表（`containGCPtrByOffset` 的 `operator[]` 会插入空条目）。
