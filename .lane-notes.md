# cjcj-llvm#23 R1

打回：`createCJMemsetLoweringLegacyPass` 仍在 `if (CJPipeline)` 内。同一函数里 `createCJBarrierLoweringPass` 已在开关外（TargetPassConfig.cpp 直接入口）。

修法：非 JIT 在 barrier lowering / ISel 之前无条件调度同一个 `lowerCJMemset`（CJSpecificOpt.cpp:125）。JIT 仍走 `createCangjieSpecificOptLegacyPass`，不另跑整套优化。不改 verifier。

锚：SelectionDAGBuilder.cpp:5931 只消费 `Intrinsic::memset`；未知 intrinsic 走 :5833。
