# cjcj-llvm#43 notes

ZGC: zAddress.inline.hpp:609-614 ZPointer::uncolor only on a load-good zpointer, `raw_addr >> load_shift_lookup(raw_addr)`. Not a constant low-48 mask.

Ours: CJBarrierLowering.cpp:886-897 uncolor() loads g_cjLoadShift and lshr. Only called from the barrier fast path (readFastPath / splitFastPathAndSlowPath).

Ours: CJBarrierLowering.cpp:1404-1406 bare memmove and value-struct raw load/store are outside the barrier set. runOnFunction only collects isCJBarrier.

Verifier: CJIRVerifier.cpp:465 ptrmask of a GC pointer is rejected. CJIRVerifier.cpp:947 shift uncolor requires a load with cj.colored.value, not a plain address.

Tests memmove-uncolor.ll and nonheap-ref-uncolor.ll still required llvm.ptrmask mask 281474976710655. That IR is illegal under P01 and is not what lowering emits. Update expectations to pass-through of the original operands, CHECK-NOT ptrmask and CHECK-NOT g_cjLoadShift.

Shift uncolor of a real barrier stays in read-barrier-uncolor.ll. Do not change that file.
