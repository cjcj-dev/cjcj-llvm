# sym_cjcj_llvm_8_implement_r5833269324

坐标基于 501694e0512adbe785f4d283809dc72207ec9d65。

| 参照 | 不变量 | 我方 |
|---|---|---|
| AArch64InstrInfo.cpp:91-93 NewArrayFast 按固定字节返回；发射 AArch64AsmPrinter.cpp:2174-2218 共 12 条 | 替换该 call MI 的指令数×4 = getInstSizeInBytes | 同函数，Callee 解析之后按名字返回 |
| AArch64AsmPrinter.cpp:2098-2116 emitMccNewObjectForCopyGC | NewObject 11 条 = 44B；NewFinalizer 再加 :2107-2113 一条 bl = 48B | getCangjieSpecificCallInstSizeInBytes 对这两个名字返回 44 / 48 |
| AsmPrinter.cpp:4172-4176 | 仅精确名 CJ_MCC_NewObject / CJ_MCC_NewFinalizer 且 EnableCJNewObjFast 才展开 | 尺寸分路用 equals，不匹配 *Fast |
| isCJStackCheck AArch64InstrInfo.cpp:123-126 | 名字分路在 Callee 解析之后；statepoint target 已在 :89-94 解进 MOSym | 不加 statepoint 专用分支，不加 GotExtra |

HotSpot TLAB 序列锚：barrierSetC2.cpp:763-780。本条是 LLVM 分支松弛记账，不是 ZGC 相位。

# cjcj-llvm#23 R1

打回：createCJMemsetLoweringLegacyPass 仍在 if (CJPipeline) 内。同一函数里 createCJBarrierLoweringPass 已在开关外。

修法：非 JIT 在 barrier lowering / ISel 之前调度同一个 lowerCJMemset（CJSpecificOpt.cpp:125）。JIT 仍走 createCangjieSpecificOptLegacyPass。不改 verifier。

锚：SelectionDAGBuilder.cpp:5931 只消费 Intrinsic::memset；未知 intrinsic 走 :5833。direct 入口是 TargetPassConfig.cpp:addISelPasses。

# cjcj-llvm#32

坐标：/root/cj_build/llvm_rebase origin/main = f85f5c9d63336dbd7a730eab709c5073c8e7c695（rc=0）。cjcjdev/main 同 sha。

上游 RewriteStatepointsForGC.cpp:638：ExtractElementInst 是标量 BDV，返回自身，不递归向量操作数。
我方 base 推导已同形：CJRewriteStatepoint.cpp:683。
撞断言的是 findAllocaInsts else 白名单（CJGCLiveAnalysis.cpp:105-108），缺 ExtractElementInst。
修法：并入与 LoadInst 相同的终端类，共用随后的 IsGCPtr 尾部。不改 :71，不递归向量操作数，不改 FileCheck。

producer → consumer：
1. 测试 IR extractelement（base-vector.ll:18，scalar-base-vector-2.ll:46）
2. computePHINodeToAllocaInsts（CJRewriteStatepoint.cpp:2141）或 computeFakeGCPtrs（CJGCLiveAnalysis.cpp:252）
3. else 终端分类必须在 :108 之前接受 ExtractElementInst
4. findBaseDefiningValue（CJRewriteStatepoint.cpp:683）与物化（:1112）产出 base_ee / gc-live，由 FileCheck 观察

# cjcj-llvm#46

坐标：/root/cj_build/llvm_rebase origin/main = 722ae417f6cb99a25286e0a4f310e8a7dc4cd9fb（rc=0）。

上游分路：RewriteStatepointsForGC.cpp:482 入口 isPtrOrPtrVectorTy；:488 向量交给 findBaseDefiningValueOfVector（:382）。向量 BDV 不是栈 alloca。ExtractElement 仍是标量终点（:638），不进 OfVector。record/struct 不在 isPtrOrPtrVectorTy 里（#30）。

我方：findAllocaInsts 入口原为 isPointerTy（CJGCLiveAnalysis.cpp:71）。recomputeLiveInValues（:1420）把 isHandledGCPointerType 的向量活值送进来。修法：入口改为 isPtrOrPtrVectorTy，向量分路 findAllocaInstsOfVector，终端类返回 false（不是 alloca）。不改 else 白名单，不加 isStructTy。
